# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные и модели данных

В этом разделе описаны интерфейсы, используемые в приложении, и классы-модели.

#### Интерфейс товара (IProduct)

Описывает структуру карточки товара, получаемую с сервера.
id: string - уникальный идентификатор;
description: string - описание товара;
image: string - ссылка на изображение;
title: string - название товара;
category: string - категория товара;
price: number | null - цена товара;

#### Интерфейс покупателя (IBuyer)

Описывает данные, которые пользователь вводит при оформлении заказа.
payment: TPayment - способ оплаты;
email: string - электронная почта;
phone: string - мобильный телефон;
address: string - адрес доставки

#### Класс ProductData (Каталог товаров)

Отвечает за хранение списка всех товаров и управление состоянием выбранного товара для просмотра в модальном окне.
Конструктор:
constructor() - инициализирует пустой массив товаров.
Поля:
\_products: IProduct[] - массив всех товаров в магазине.
\_preview: IProduct | null - товар, выбранный для отображения.
Методы:
setProducts(items: IProduct[]): void - сохраняет массив товаров в модель.

getProducts(): IProduct[] - возвращает список всех товаров.

getProduct(id: string): IProduct - находит и возвращает товар по его ID.

setPreview(item: IProduct): void - устанавливает товар для отображения в модальном окне.

getPreview(): IProduct - возвращает текущий выбранный для предпросмотра товар.

#### Класс BasketData (Корзина)

Управляет списком товаров, которые пользователь добавил для покупки, и рассчитывает итоговые показатели.
Конструктор:
constructor() - инициализирует пустой массив выбранных товаров.
Поля:
\_items: IProduct[] - массив товаров в корзине.
Методы:
getItems(): IProduct[] - возвращает список всех товаров в корзине.

addItem(item: IProduct): void - добавляет новый товар в корзину.

removeItem(id: string): void - удаляет товар из корзины по его ID.

clearBasket(): void - полностью очищает корзину.

getTotalPrice(): number - вычисляет и возвращает суммарную стоимость всех товаров.

getCounter(): number - возвращает количество товаров в корзине.

inBasket(id: string): boolean - проверяет, есть ли уже товар с таким ID в корзине.

#### Класс UserData (Данные покупателя)

Отвечает за хранение информации о пользователе и проверку её корректности перед отправкой на сервер.
Конструктор:
constructor() - устанавливает начальные пустые значения для всех полей.
Поля:
payment: TPayment - вид оплаты.

address: string - адрес доставки.

email: string - почта.

phone: string - телефон.

formErrors: Partial<Record<keyof IBuyer, string>> - объект с описанием ошибок валидации.
Методы:
setUserData(field: keyof IBuyer, value: string): void - сохраняет значение в конкретное поле модели.

getUserData(): IBuyer - возвращает объект со всеми данными покупателя.

clearUserData(): void - сбрасывает все поля после оформления заказа.

validateUserData(): boolean - проверяет заполненность полей.

### Слой коммуникации: Класс AppApi

Наследует базовый класс Api, использует его методы для взаимодействия с конкретными эндпоинтами сервера.
Конструктор:
constructor(baseUrl: string, options?: RequestInit) - принимает базовый URL сервера и настройки запросов.
Методы:
getProductList(): Promise<IProduct[]> - получает массив товаров с сервера, подставляя полный путь к картинкам.

orderProducts(order: IOrder): Promise<IOrderResult> - отправляет данные заказа на сервер и возвращает результат.

Хорошо, вот простая документация для копирования в README:

---

# Документация классов слоя Представления (View)

## Базовые классы

### CardBase

Назначение: базовый класс для всех карточек товара. Содержит общий функционал: установка заголовка и цены.

Поля:
- `_title: HTMLElement` - элемент заголовка
- `_price: HTMLElement` - элемент цены

Конструктор: `constructor(container: HTMLElement, actions?: ICardActions)` - находит элементы `.card__title` и `.card__price`, устанавливает слушатель клика на контейнер.

Методы:
- `set title(value: string)` - устанавливает заголовок
- `set price(value: number | null)` - устанавливает цену, при null показывает "Бесценно"

---

### FormBase

Назначение: базовый класс для всех форм. Содержит общий функционал: управление кнопкой submit, отображение ошибок.

Поля:
- `_form: HTMLFormElement` - элемент формы
- `_submitButton: HTMLButtonElement` - кнопка отправки
- `_errorsContainer: HTMLElement` - контейнер для ошибок

Конструктор: `constructor(container: HTMLElement)` - находит элементы, устанавливает слушатели на input и submit.

Методы:
- `set valid(value: boolean)` - включает/выключает кнопку submit
- `set errors(value: string)` - отображает текст ошибок
- `protected onInputChange(): void` - переопределяется в наследниках
- `protected onSubmit(): void` - переопределяется в наследниках

---

## Классы карточек (наследники CardBase)

### CardCatalog

Назначение: карточка для галереи каталога.

Поля:
- `_category: HTMLElement` - элемент категории
- `_image: HTMLImageElement` - элемент изображения

Методы:
- `set category(value: string)` - устанавливает категорию и модификатор из categoryMap
- `set image(value: string)` - устанавливает изображение
- `render(product: IProduct): HTMLElement` - отображает карточку

---

### CardPreview

Назначение: карточка для модального окна (полная версия).

Поля:
- `_category: HTMLElement` - элемент категории
- `_image: HTMLImageElement` - элемент изображения
- `_description: HTMLElement` - элемент описания
- `_button: HTMLButtonElement` - кнопка действия

Методы:
- `set category(value: string)` - устанавливает категорию и модификатор
- `set image(value: string)` - устанавливает изображение
- `set description(value: string)` - устанавливает описание
- `set buttonText(value: string)` - устанавливает текст кнопки
- `set buttonDisabled(value: boolean)` - блокирует/разблокирует кнопку
- `render(product: IProduct & { buttonText?: string; buttonDisabled?: boolean }): HTMLElement` - отображает карточку

---

### CardBasket

Назначение: карточка для корзины (компактная версия).

Поля:
- `_index: HTMLElement` - номер товара в корзине
- `_deleteButton: HTMLButtonElement` - кнопка удаления

Методы:
- `set index(value: number)` - устанавливает номер товара
- `render(product: IProduct & { index: number }): HTMLElement` - отображает товар

---

## Классы форм (наследники FormBase)

### OrderForm

Назначение: первая форма оформления заказа (способ оплаты и адрес).

Поля:
- `_cardButton: HTMLButtonElement` - кнопка оплаты онлайн
- `_cashButton: HTMLButtonElement` - кнопка оплаты при получении
- `_addressInput: HTMLInputElement` - поле ввода адреса

Методы:
- `set payment(value: string)` - устанавливает выбранный способ оплаты (online/offline)
- `set address(value: string)` - устанавливает адрес
- `set errors(value: string)` - отображает ошибки
- `render(data: IOrderFormData): HTMLElement` - отображает форму

Генерируемые события:
- `order:paymentSelected` - при выборе способа оплаты
- `order:addressChanged` - при изменении адреса
- `order:submit` - при отправке формы

---

### ContactsForm

Назначение: вторая форма оформления заказа (email и телефон).

Поля:
- `_emailInput: HTMLInputElement` - поле ввода email
- `_phoneInput: HTMLInputElement` - поле ввода телефона

Методы:
- `set email(value: string)` - устанавливает email
- `set phone(value: string)` - устанавливает телефон
- `set errors(value: string)` - отображает ошибки
- `render(data: IContactsFormData): HTMLElement` - отображает форму

Генерируемые события:
- `contacts:emailChanged` - при изменении email
- `contacts:phoneChanged` - при изменении телефона
- `contacts:submit` - при отправке формы

---

## Самостоятельные компоненты

### Page

Назначение: управление галереей и счётчиком корзины в хедере.

Поля:
- `_counter: HTMLElement` - счётчик корзины
- `_gallery: HTMLElement` - контейнер галереи
- `_basketButton: HTMLElement` - кнопка корзины

Методы:
- `set counter(value: number)` - устанавливает счётчик
- `set items(items: HTMLElement[])` - заполняет галерею

Генерируемые события:
- `basket:open` - при клике на кнопку корзины

---

### Modal

Назначение: модальное окно.

Поля:
- `_closeButton: HTMLButtonElement` - кнопка закрытия
- `_content: HTMLElement` - контейнер для контента
- `_container: HTMLElement` - корневой элемент

Методы:
- `set content(value: HTMLElement)` - устанавливает контент
- `open()` - открывает окно (добавляет класс `modal_active`)
- `close()` - закрывает окно и очищает контент
- `render(data: { content: HTMLElement }): HTMLElement` - устанавливает контент и открывает окно

---

### Basket

Назначение: компонент корзины (список товаров, сумма, кнопка оформления).

Поля:
- `_list: HTMLElement` - список товаров
- `_total: HTMLElement` - общая сумма
- `_button: HTMLButtonElement` - кнопка оформления

Методы:
- `set items(items: HTMLElement[])` - устанавливает список товаров
- `set total(value: number)` - устанавливает общую сумму
- `set disabled(value: boolean)` - блокирует/разблокирует кнопку

Генерируемые события:
- `order:start` - при клике на кнопку оформления

---

### Success

Назначение: компонент успешного оформления заказа.

Поля:
- `_description: HTMLElement` - элемент с суммой списания
- `_button: HTMLButtonElement` - кнопка закрытия

Методы:
- `set total(value: number)` - устанавливает сумму списания
- `render(data: ISuccessData): HTMLElement` - отображает сообщение

Генерируемые события:
- `success:close` - при клике на кнопку

---

## События приложения

### От представлений (View)

| Событие | Компонент | Описание |
|---------|-----------|----------|
| `card:select` | CardCatalog | Выбор карточки для просмотра |
| `basket:open` | Page | Открытие корзины |
| `basket:remove` | CardBasket | Удаление товара из корзины |
| `preview:buttonClick` | CardPreview | Клик по кнопке в карточке товара |
| `order:start` | Basket | Начало оформления заказа |
| `order:paymentSelected` | OrderForm | Выбор способа оплаты |
| `order:addressChanged` | OrderForm | Изменение адреса |
| `order:submit` | OrderForm | Отправка первой формы |
| `contacts:emailChanged` | ContactsForm | Изменение email |
| `contacts:phoneChanged` | ContactsForm | Изменение телефона |
| `contacts:submit` | ContactsForm | Отправка формы контактов |
| `success:close` | Success | Закрытие сообщения об успехе |

### От моделей данных (Model)

| Событие | Модель | Описание |
|---------|--------|----------|
| `products:changed` | ProductData | Изменение каталога товаров |
| `preview:changed` | ProductData | Изменение выбранного товара |
| `basket:changed` | BasketData | Изменение содержимого корзины |
| `user:changed` | UserData | Изменение данных покупателя |

---

## Презентер

Файл: `src/main.ts`

Презентер реализован в основном файле приложения. Он подписывается на все события от моделей и представлений и связывает их между собой.

Основные обработчики:

| Событие | Действие |
|---------|----------|
| `products:changed` | Создаёт карточки через CardCatalog, обновляет галерею |
| `basket:changed` | Обновляет счётчик в хедере и содержимое корзины |
| `preview:changed` | Отображает CardPreview в модальном окне |
| `card:select` | Вызывает setPreview в модели ProductData |
| `preview:buttonClick` | Добавляет/удаляет товар из корзины, закрывает модалку |
| `basket:open` | Отображает корзину в модальном окне |
| `basket:remove` | Удаляет товар из корзины |
| `order:start` | Отображает OrderForm |
| `order:paymentSelected` | Обновляет модель UserData, перерисовывает форму |
| `order:addressChanged` | Обновляет модель UserData, перерисовывает форму |
| `order:submit` | Отображает ContactsForm |
| `contacts:emailChanged` | Обновляет модель UserData, перерисовывает форму |
| `contacts:phoneChanged` | Обновляет модель UserData, перерисовывает форму |
| `contacts:submit` | Отправляет заказ на сервер, показывает Success |
| `success:close` | Закрывает модальное окно |

Принципы работы:
- Презентер не генерирует события, только обрабатывает их
- Презентер не работает с DOM напрямую
- Все изменения данных происходят через модели
- Представления перерисовываются только после изменения данных в моделях